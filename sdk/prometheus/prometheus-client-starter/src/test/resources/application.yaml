logging:
  level:
    root: debug

io:
  github:
    surezzzzzz:
      sdk:
        prometheus:
          write:
            host: ${PROMETHEUS_HOST:http://localhost:9000}
          read:
            host: ${PROMETHEUS_HOST:http://localhost:9000}
