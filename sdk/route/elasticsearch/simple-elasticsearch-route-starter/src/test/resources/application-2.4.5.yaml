# Spring Boot 2.4.5 测试 profile。
# primary 继承 application.yaml 的 9203，secondary 使用 ES 7.9.3（9204）。

io:
  github:
    surezzzzzz:
      sdk:
        elasticsearch:
          route:
            sources:
              secondary:
                urls: http://localhost:9204
