spring:
  profiles:
    active: local

io:
  github:
    surezzzzzz:
      sdk:
        auth:
          aksk:
            client:
              enable: true
              server-url: http://localhost:8080
              token-endpoint: /oauth2/token

logging:
  level:
    io.github.surezzzzzz.sdk: DEBUG
